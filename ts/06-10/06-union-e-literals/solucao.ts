let transacaoId: string | number;
transacaoId = "TX-123";

type EstadoCivil = "solteiro" | "casado" | "divorciado";

let meuEstado: EstadoCivil = "solteiro";

console.log(`ID: ${transacaoId} | Estado: ${meuEstado}`);
